
import 'package:flutter/material.dart';

import 'body_of_edit.dart';

class Edit extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: bodyedit(),
    );
  }
}
